using UnityEngine;
using System.Collections;

public class DownloadMatierals : MonoBehaviour {
	/*
	public string url;
	WWW www;
	Material mat;
	public GameObject cube;
	// Use this for initialization
	void Start () {
		www = new WWW (url);
		StartCoroutine (wait);
	}

	public IEnumerator wait(){
		yield return www;
		mat = www.LoadImageIntoTexture;
	}
	// Update is called once per frame
	void Update () {
		if(www.isDone){
			cube.renderer.material
		}
	}
	*/
}
